mysql>
create database midawn;
grant all privileges on midawn.* to "midawner"@"localhost" identified by "midawner";
flush privileges; 

notice: nl2br(), addslashes(), (stripslashes())
Ipsum to generate sample article

table:
md_news:
#id 
title
author
content
timestamp
date/time to show
category

CREATE TABLE `midawn`.`md_news` (
  `newsID` INT NOT NULL AUTO_INCREMENT,
  `title` VARCHAR(200) NULL,
  `author` VARCHAR(100) NULL,
  `content` TEXT NULL,
  `createTime` TIMESTAMP NULL,
  `updateTime` TIMESTAMP NULL,
  `category` VARCHAR(100) NULL,
  PRIMARY KEY (`newsID`));
  
 task: implement news
 how?
 databse set up: done
 test php show database: done
 test write into database through php page:
     a page to convey through form: done
     Produce data(about 20) , with limited authors(inwolf, Bri'an C, Raisive Crowx):
     change date for filter test:
 