mysql>
create database midawn;
grant all privileges on midawn.* to "midawner"@"localhost" identified by "midawner";
flush privileges; 

notice: nl2br(), addslashes(), (stripslashes())
Ipsum to generate sample article


NEWS

table:
md_news:
#id 
title
author
content
timestamp
date/time to show
category

CREATE TABLE `midawn`.`md_news` (
  `newsID` INT NOT NULL AUTO_INCREMENT,
  `title` VARCHAR(200) NULL,
  `author` VARCHAR(100) NULL,
  `content` TEXT NULL,
  `createTime` TIMESTAMP NULL,
  `updateTime` TIMESTAMP NULL,
  `category` VARCHAR(100) NULL,
  PRIMARY KEY (`newsID`));
  
 task: implement news
 how?
 database set up: done
 test php show database: done
 test write into database through php page: done
     a page to convey through form: done
     Produce data(about 10) , with limited authors(inwolf, Bri'an C, Raisive Crowx): done
     change date for filter test: done
 A page with display function: 
     Trim an article to limited summary: done
     Sidebar with filter function:
         Display filter: done
     			fetch month and date: done
     			fetch category: done
     		Enable filter: done
     Title and Read More into single post : done
     misc: done


About US
     
 CREATE TABLE `midawn`.`md_about` (
  `aboutID` INT NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(100) NULL,
  `detail` TEXT NULL,
  `codename` VARCHAR(10) NULL,
  `category` VARCHAR(45) NULL,
  `icon` VARCHAR(100) NULL,
  `picture` VARCHAR(100) NULL,
  `timestamp` TIMESTAMP NOT NULL DEFAULT NOW(),
  `joinDate` DATE NULL,
  PRIMARY KEY (`aboutID`));
  
  ALTER TABLE `midawn`.`md_about` 
  CHANGE COLUMN `codename` `codename` VARCHAR(10) NULL AFTER `name`,
  ADD COLUMN `position` VARCHAR(100) NULL AFTER `codename`;
  
Gallery

CREATE TABLE `midawn`.`md_gallery` (
  `galleryID` INT NOT NULL AUTO_INCREMENT,
  `description` VARCHAR(400) NULL,
  `category` VARCHAR(45) NULL,
  `location` VARCHAR(100) NULL,
  `timestamp` TIMESTAMP NULL DEFAULT NOW(),
  `pictureDate` DATE NULL,
  PRIMARY KEY (`galleryID`));

